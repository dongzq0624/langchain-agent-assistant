# =============================================
# 前端环境变量模板
# =============================================
#
# 使用方法：
#   1. 复制本文件为 .env.development（开发环境）或 .env.production（生产环境）
#   2. 根据实际情况修改配置
#   3. .env.* 文件不会提交到 Git（已在 .gitignore 中排除）
#
# 环境变量加载优先级（Vite 规则）：
#   .env                    ← 始终加载（基础配置）
#   .env.local              ← 始终加载，优先级更高（本地覆盖）
#   .env.[mode]             ← 根据 --mode 加载对应文件
#   .env.[mode].local       ← 根据 --mode 加载，优先级最高
#
# 常用命令：
#   pnpm dev                ← 使用 .env.development（开发模式）
#   pnpm build              ← 使用 .env.production（生产模式，默认）
#   pnpm build --mode xxx   ← 使用 .env.xxx
#
# =============================================

# API 地址配置
# ================
#
# 留空：使用相对路径（如 /api/chat/stream）
#   - 开发环境：通过 Vite proxy 代理到后端
#   - 生产环境：通过 Nginx proxy 代理到后端
#
# 设置为绝对 URL：直接请求指定后端地址（跳过代理）
#   - 示例：VITE_API_BASE_URL=http://101.37.236.214:3000
#
VITE_API_BASE_URL=
